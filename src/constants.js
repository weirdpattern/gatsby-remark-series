/**
 * Default series toc cache key.
 */
export const SERIES_TOC_CACHE_KEY = "__GATSBY_REMARK_SERIES_TOC";

/**
 * Default settings for the series toc.
 */
export const DefaultSettings = {
  field: "series"
};
